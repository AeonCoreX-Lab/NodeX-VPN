<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <key>CFBundleDisplayName</key>
    <string>NodeX TV Proxy</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleIdentifier</key>
    <string>com.nodex.vpn.tvos.proxy</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>CFBundleName</key>
    <string>NodeXTvTunnel</string>

    <key>CFBundlePackageType</key>
    <string>XPC!</string>

    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <!--
        tvOS does NOT support NEPacketTunnelProvider (full packet tunnel extension).
        tvOS DOES support NEAppProxyProvider — it proxies TCP/UDP connections
        for specific apps through a SOCKS5/HTTP proxy.

        Strategy: NodeXTvTunnel runs an NEAppProxyProvider that forwards all
        Apple TV traffic to the Tor SOCKS5 proxy running on the user's iPhone
        (which runs the full NodeX VPN). The iPhone IP is discovered via
        Bonjour/mDNS (_nodex-vpn._tcp.local) or manual entry in Settings.
    -->
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.networkextension.app-proxy</string>

        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).NodeXAppProxyProvider</string>
    </dict>

    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
</dict>
</plist>
